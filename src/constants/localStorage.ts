const version = "v1";

export const localStorageKey = {
  accessToken: `accessToken_${version}`,
  refreshToken: `refreshToken_${version}`,
  userId: `userId_${version}`,
  role: `role_${version}`,
  roomId: `roomId_${version}`,
};
